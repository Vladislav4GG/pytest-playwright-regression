[pytest]
addopts = -q -ra
testpaths = tests
markers =
    smoke: fast critical checks
    regression: broader suite
    returns: return flow checks
    smoke: fast smoke tests
    e2e: full end-to-end tests with real order creation
